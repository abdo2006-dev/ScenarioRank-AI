# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
!.env.example

# Evaluation run artifacts (Phase 3A). Runs are reproducible from the
# committed benchmark plus a commit hash, and their output is large, machine-
# specific in its timings, and — for live runs — derived from paid API calls.
# Nothing here is ever committed; see docs/evaluation/RUNBOOK.md.
.eval-runs/
